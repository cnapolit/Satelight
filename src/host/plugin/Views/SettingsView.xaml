<UserControl x:Class="HostPlugin.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:m="clr-namespace:HostPlugin.Views.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance Type=m:SettingsModel}">
    <StackPanel Margin="2">
        <CheckBox Content="IsMaster" IsChecked="{Binding Settings.IsMaster}"/>
        <TextBlock Text="Port"/>
        <TextBox   Text="{Binding Settings.Port}"/>
        <TextBlock Text="Ip Address"/>
        <TextBox   Text="{Binding Host.IpAddress}"/>
        <TextBlock Text="Host Name"/>
        <TextBox   Text="{Binding Host.Name}"/>
        <TextBlock Text="Host Port"/>
        <TextBox   Text="{Binding Host.Port}"/>
    </StackPanel>
</UserControl>
